import type{PageBlock,PageDocument,PageSection}from"@/src/website/page-document";const cloneBlock=(block:PageBlock):PageBlock=>({...block,id:crypto.randomUUID(),settings:{...block.settings},responsiveStyles:block.responsiveStyles?structuredClone(block.responsiveStyles):undefined,blocks:block.blocks?.map(cloneBlock)});export const insertSection=(document:PageDocument,section:PageSection)=>({...document,sections:[...document.sections,section]});export const duplicateSection=(document:PageDocument,id:string)=>{const index=document.sections.findIndex(section=>section.id===id);if(index<0)return document;const copy=cloneBlock(document.sections[index])as PageSection;return{...document,sections:[...document.sections.slice(0,index+1),copy,...document.sections.slice(index+1)]}};export const removeSection=(document:PageDocument,id:string)=>({...document,sections:document.sections.filter(section=>section.id!==id)});export const reorderSection=(document:PageDocument,id:string,direction:-1|1)=>{const from=document.sections.findIndex(section=>section.id===id),to=from+direction;if(from<0||to<0||to>=document.sections.length)return document;const sections=[...document.sections];[sections[from],sections[to]]=[sections[to],sections[from]];return{...document,sections}};export type DocumentHistory={past:PageDocument[];present:PageDocument;future:PageDocument[]};export const historyStart=(document:PageDocument):DocumentHistory=>({past:[],present:document,future:[]});export const historyCommit=(history:DocumentHistory,document:PageDocument):DocumentHistory=>({past:[...history.past.slice(-49),history.present],present:document,future:[]});export const historyUndo=(history:DocumentHistory):DocumentHistory=>history.past.length?{past:history.past.slice(0,-1),present:history.past.at(-1)!,future:[history.present,...history.future]}:history;export const historyRedo=(history:DocumentHistory):DocumentHistory=>history.future.length?{past:[...history.past,history.present].slice(-50),present:history.future[0],future:history.future.slice(1)}:history;
