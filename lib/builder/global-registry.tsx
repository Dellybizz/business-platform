import type{GlobalDefinition,GlobalModule}from"./types";import{validateComponentManifest}from"./manifest-validation";const modules=(import.meta as ImportMeta&{glob:(pattern:string[],options:{eager:true})=>Record<string,unknown>}).glob(["./globals/*.tsx","./globals/*/index.tsx"],{eager:true})as Record<string,GlobalModule>;export const globalRegistry=Object.fromEntries(Object.entries(modules).map(([path,module])=>{const d=validateComponentManifest(module.default,path);return[d.type,d]}))as Record<string,GlobalDefinition>;
