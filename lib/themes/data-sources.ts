import type{ThemeDataRole,ThemeManifest}from"./registry";import{themeRegistry}from"./registry";import{createSection,sectionRegistry}from"@/lib/builder/registry";export type DynamicThemeData={products?:unknown[];collections?:unknown[];services?:unknown[];projects?:unknown[];resume?:unknown};export function validateThemeLayouts(theme:ThemeManifest){for(const template of theme.templates)for(const section of template.sections)if(!sectionRegistry[section.component])throw new Error(`Theme ${theme.id} template ${template.id} references unknown component ${section.component}`);return theme}export function bindThemeData(role:ThemeDataRole|undefined,data:DynamicThemeData){if(!role)return null;const values:Record<ThemeDataRole,unknown>={"featured-products":data.products||[],"featured-collection":data.collections||[],services:data.services||[],projects:data.projects||[],resume:data.resume||null};return values[role]}export function resolveThemeLayout(themeId:string,templateId:string,data:DynamicThemeData){const manifest=themeRegistry[themeId];if(!manifest)throw new Error("Unknown theme");const theme=validateThemeLayouts(manifest);const template=theme.templates.find(item=>item.id===templateId);if(!template)throw new Error("Unknown theme template");return template.sections.map(item=>({section:createSection(item.component,item.preset||0),dataRole:item.dataRole||null,data:bindThemeData(item.dataRole,data)}))}
