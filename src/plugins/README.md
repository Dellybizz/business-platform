# Plugins

Plugin discovery, lifecycle, permissions, extension targets and first-party packages. Plugins use published service APIs and namespaced storage; direct infrastructure/database access is forbidden.

